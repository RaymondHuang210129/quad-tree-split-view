#include <iostream>

#include <glad/glad.h> // Check if this is correctly imported
#include <GLFW/glfw3.h> // Check if this is correctly imported
#include <glm/vec2.hpp> // Check if this is correctly imported

import sample;

int main() {
    std::cout << helloWorld() << std::endl;
    return 0;
}